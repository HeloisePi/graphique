{
	"name": "teampinpin",
	"version": "0.0.1",
	"scripts": {
		"dev": "npm run dev",
		"build": "npm run build",
		"preview": "npm run preview",
		"lint": "prettier --plugin-search-dir . --check .",
		"format": "prettier --plugin-search-dir . --write ."
	},
	"devDependencies": {
		"@sveltejs/kit": "^1.20.4",
		"prettier": "^2.8.0",
		"sass": "^1.69.4",
		"svelte": "^4.0.5",
		"vite": "^4.4.2"
	},
	"type": "module",
	"vite": {
		"build": {
			"ssr": {
				"skipAnalysis": true
			}
		}
	}
}
